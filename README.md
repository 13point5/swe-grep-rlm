# SWE-Grep Recursive Language Model Environment

A Verifiers environment to perform code-search like Cognition's SWE-Grep model using Recursive Language Models as the harness.

This is an experiment to see if we can make small 4B models perform as well as large models by managing their context window effectively
using the RLM scaffold.
